import styled from "styled-components";

const NonTaskWrapper = styled.div`
  background-color: ${({ theme }) => theme.palette.default.light};
  min-height: 100vh;
  font-size: 1.6rem;
`;

export default NonTaskWrapper;
