// TODO: Create time utils function
/**
 * This will include stuff like
 * isTheSameTime, isBefore, isAfter, isBetween => boolean
 * getDateBefore, getDateAfter(date, value, type): type will be stuff like months, date etc, while value is the numerical value while it will use the other date value e.g.
 * getDateAfter(new Date("24-Aug-2021"), 8, "month") will return 24-Sept-2021 because 8 in months is sept while getDateBefore(new Date("24-Aug-2021"), 8, "month") will return 24-Sept-2020
 * There will also be getPrevDate & getNextDate(date, value, type). This will be good for things like weeks, getNextDate(new Date("2018-06-27"), 2, "week") will return 2 weeks in the future
 * ie. 2018-07-11
 * getHumanReadableDistance(date, now?) => it will find the difference between the date and now (or second variable) and will return stuff like "tomorrow", "in 20 minutes", "Next year"
 * @returns
 */
export default function DateTimeUtils() {
  return true;
}
