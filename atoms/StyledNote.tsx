import styled from "styled-components";

export default styled.div`
  border-left: 0.4rem solid ${({ theme }) => theme.palette.primary.main};
  padding-left: 0.4rem;
  margin-left: 0.4rem;
  margin-bottom: 0.4rem;
  font-style: italic;
`;
