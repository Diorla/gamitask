import styled from "styled-components";

/**
 * Everything is placed on top of each other
 */
const Stack = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1;
`;

export default Stack;
